SELECT 
  wbi.bundled_item_id,
  wbi.product_id,
  wbi.bundle_id,
  wbi.menu_order,
  child.post_title as child_post_title
FROM wp_woocommerce_bundled_items wbi
JOIN wp_posts child ON wbi.product_id = child.ID
WHERE wbi.bundle_id IN ({{bundleIds}})
ORDER BY wbi.bundle_id, wbi.menu_order;